interface:
  display_name: "GPT-only 路由"
  short_description: "禁止 Claude，Sol Medium 主控，最多两个获批代理"
  default_prompt: "使用 $sol-gpt-orchestrator 启用 GPT-only；禁止 Claude，未经我明确批准不得启动 subagent。"
policy:
  allow_implicit_invocation: false

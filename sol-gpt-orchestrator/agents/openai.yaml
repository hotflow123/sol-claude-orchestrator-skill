interface:
  display_name: "Sol GPT 家族编排"
  short_description: "仅使用 GPT 家族，由 Sol 主控并最多追加一个模型"
  default_prompt: "使用 $sol-gpt-orchestrator 仅以 GPT 家族、最多双模型处理当前任务。"
policy:
  allow_implicit_invocation: false

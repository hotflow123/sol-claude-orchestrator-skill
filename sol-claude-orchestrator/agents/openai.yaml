interface:
  display_name: "Sol + Claude 双核编排"
  short_description: "Sol 主控，按风险最多追加一个 Claude 或 GPT 模型复核"
  default_prompt: "使用 $sol-claude-orchestrator 以最多双模型、XHigh 默认级别处理当前任务。"
policy:
  allow_implicit_invocation: false
